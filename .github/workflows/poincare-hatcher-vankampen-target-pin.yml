name: Poincare Hatcher Van Kampen target-pin replay

on:
  push:
    branches:
      - tmp-poincare-hatcher-vankampen-target-pin-v3-20260917

permissions:
  contents: read

jobs:
  hatcher-vankampen-target-pin:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout harness
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Run exact-pin Hatcher migration through full rectangle row composition
        shell: bash
        run: bash diagnostics/run-hatcher-rectangle-composition.sh
